# 12 — 发布通用 JSON/YAML 测试任务

**What to build:** 测试工程师选择已确认用例、执行目标和目标扩展，通过发布确认生成版本化的通用测试任务，
并验证它可通过执行适配器转换为测试执行与诊断平台目标契约而不污染通用领域模型。

**Blocked by:** 11 — 治理用例身份、状态和单独下载。

**Status:** resolved

- [x] 通用测试任务包含任务身份、用例及修订引用、目标、优先级、前置条件、参数、预期结果、判定方式和证据要求。
- [x] 执行目标可表达人工、测试执行与诊断平台、通用自动化框架、其他外部执行器或暂未指定。
- [x] 特定执行目标字段只存在于目标扩展，不成为所有测试任务的必需字段。
- [x] JSON 和 YAML 导出使用同一版本化契约，并通过 Schema 校验。
- [x] 发布确认前不能生成正式测试任务；确认记录包含执行范围、执行目标和确认人。
- [x] 测试执行与诊断平台执行适配器完成通用任务到目标契约、目标结果到运行结果反馈的往返。
- [x] 往返保留稳定用例 ID、用例修订版本、任务身份、判定语义和证据要求或引用。
- [x] 无测试执行与诊断平台服务时，通用任务仍可独立生成、下载和校验。
- [x] API、适配器契约和前端关键流程测试覆盖发布成功、未确认、非法目标扩展和版本不兼容。

## Comments

- 2026-08-24: 从 V1 实施规格拆分并发布；遵守“测试任务与执行目标解耦”ADR。
- 2026-08-25: 已实现并验证通用测试任务发布、JSON/YAML 契约、目标扩展校验、执行适配器往返和前端发布闭环。

## Answer

已完成任务 12。后端以 FastAPI API 为主测试缝，通用任务独立于目标扩展；测试执行与诊断平台只通过执行适配器接入。

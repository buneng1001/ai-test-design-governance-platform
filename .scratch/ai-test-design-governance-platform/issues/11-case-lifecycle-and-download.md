# 11 — 治理用例身份、状态和单独下载

**What to build:** 测试工程师可长期治理已确认测试用例的身份、修订、生命周期和当前测试参与状态，
并按照确认的多工作表模板单独下载不泄漏内部设计信息的用例文件。

**Blocked by:** 08 — 完成多工作表用例模板映射；10 — 完成三角色 AI 独立评审与用例确认。

**Status:** resolved

- [ ] 稳定用例 ID 跨修订保持不变，外部用例编号保留但不替代内部身份。
- [ ] 测试目标根本变化时创建新稳定用例 ID，并要求旧用例关联承接用例后标记为被替代。
- [ ] 用例生命周期支持草稿、有效、已关闭、已废弃和被替代，并遵守正式迁移约束。
- [ ] 当前测试参与状态支持已纳入、本轮不测、待影响确认和待复测，与生命周期独立管理。
- [ ] 每次用例状态变更记录原状态、新状态、原因、时间和确认人；AI 只能提出建议。
- [ ] 用例导出范围支持当前全部、人工勾选及相对上一需求版本新增或修改。
- [ ] 默认只导出最新、已确认且已纳入的用例修订版本，并保持每条用例唯一归属表。
- [ ] 非参与工作表按模板边界保留；用例设计依据、AI 评审记录、密钥和任务信息不进入用例文件。
- [ ] XLSX/CSV 往返测试验证身份、修订、字段映射、工作表保留和敏感内部字段排除。
- [ ] 前端测试覆盖状态变更和独立下载用例文件。

## Comments

- 2026-08-24: 从 V1 实施规格拆分并发布。

## Answer

- 已在 `ticket/11-case-lifecycle-and-download` 实现稳定用例 ID、用例修订版本、用例生命周期和当前测试参与状态。
- 已增加人工状态变更审计、被替代用例承接关系，以及当前全部、人工勾选和新增/修改范围导出。
- 已按已确认模板生成 CSV/XLSX 用例文件，并保留非用例工作表，排除用例设计依据、AI 评审记录和测试任务信息。
- 验证：后端 `43 passed`，前端 `6 passed`，`pnpm build` 通过。

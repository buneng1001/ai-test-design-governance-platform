# 项目 Agent 规则

## Agent skills

### Issue tracker

本项目使用 `.scratch/` 下的本地 Markdown 文件管理规格和任务。详见 `docs/agents/issue-tracker.md`。

### Triage labels

本项目使用默认的五种 triage 状态。详见 `docs/agents/triage-labels.md`。

### Domain docs

本项目采用 single-context 领域文档结构。详见 `docs/agents/domain.md`。

# AI 测试设计与治理平台规格

## 1. 项目定位

AI Test Design and Governance Platform（AI 测试设计与治理平台）面向 Python 自动化测试工程师岗位，
把需求资料转化为可评审、可追踪、可执行和可持续治理的测试设计资产。

首个演示测试对象是虚构的智能采集设备，但平台不得绑定设备专项。接口服务、Web 系统、移动应用和其他设备
应能通过新建测试设计项目、上传资料和配置测试维度复用同一工作流。

## 2. 洁净重写边界

- 从空目录独立设计和实现。
- 不读取、复制或改造原公司的代码、Prompt、页面、数据、文档和未公开信息。
- 个人工作总结只用于提炼通用测试方法，不复用真实型号、版本、接口、阈值、缺陷数据或厂商信息。
- 演示输入、Mock 输出和评估真值必须登记资产来源、许可、模型使用权限和内容哈希。
- 来源不明资产不得进入仓库、演示数据或模型上下文。

## 3. 与项目一的边界

项目一“AI 智能硬件测试执行与诊断平台”负责：

- 执行测试任务。
- 生成或采集视频、IMU、状态和日志。
- 运行视频、IMU、同步和文件完整性等确定性检查。
- 汇总人工结果、形成证据包并进行 AI 辅助诊断。

本项目负责：

- 解析需求和产品资料。
- AI 辅助需求评审、范围分析和风险分析。
- 识别自动化候选并设计测试用例。
- 通过三种 AI 角色评审用例，由人工决定是否采纳建议。
- 导出通用 JSON/YAML 测试任务及项目一目标契约。
- 接收人工和自动化结果，治理覆盖、缺陷模式与回归用例。

两个项目独立运行，通过稳定契约对接。本项目不实现设备控制、数据生成和多模态检查器。

## 4. 使用者与运行环境

- 首版使用者为单个本地测试工程师。
- 产品经理、测试经理和项目经理是 AI 评审视角，不是真实账户角色。
- 首版不实现登录、团队、权限、多人并发、消息通知和正式审批流。
- 人工确认记录允许填写确认人名称用于演示审计。

## 5. 核心闭环

1. 创建测试设计项目并描述测试对象。
2. 导入一份或多份需求资料，形成需求版本。
3. AI 提取原子需求、来源引用和需求评审发现。
4. 人工完成需求确认。
5. AI 提取测试范围、风险和自动化候选。
6. 人工完成设计确认。
7. 上传多工作表用例模板并确认逐表字段映射。
8. AI 生成测试用例和内部设计依据。
9. 产品经理、测试经理和项目经理三个 AI 评审员独立评审。
10. 人工逐条采纳、拒绝或修改建议，形成用例修订版本并完成用例确认。
11. 单独下载用例文件，或在发布确认后导出目标测试任务。
12. 创建执行批次，导出人工执行文件并导入多份人工或自动化结果。
13. 处理阻塞、执行异常、复测和结果冲突，形成批次结论。
14. 导入 V2 需求，分析变更影响，治理用例状态并确认回归选择。

## 6. 需求资料与版本

首版支持：

- Markdown、TXT、DOCX 和可提取文本的 PDF。
- JSON、YAML 和 OpenAPI。
- PNG、JPG 页面截图；识别内容必须标记为视觉推断并人工确认。

首版不承诺扫描版长 PDF 的完整 OCR、视频需求讲解、在线文档同步或任意私有格式。

一个需求版本是需求资料包的不可覆盖快照。AI 将复合描述拆成具有稳定需求 ID 的原子需求，保留原始来源引用；
人工可以调整拆分，但不得改写原始资料。

需求评审发现类型包括歧义、遗漏、冲突、不可测试、缺少验收标准、依赖未明确、约束或异常处理缺失、
视觉推断待确认和其他。状态包括待确认、已采纳、已拒绝、待外部确认、已解决和接受风险。

## 7. 测试维度、范围与风险

平台提供功能、接口、数据、UI、性能、稳定性与压力、异常恢复、兼容性、安全与数据保护、安装升级回滚、
端到端和可用性等基础维度。项目可以增加设备或业务特有子维度。

- 项目设置负责完整维护。
- 需求评审可产生 AI 维度建议。
- 设计确认后建议才生效。
- 已使用维度可改名、移动、合并或停用，不可直接删除。
- 一个范围项有一个主要维度和多个次要维度，覆盖统计按范围项去重。

风险采用可解释分项评分。通用因子包括业务影响、发生可能性、可发现性和影响范围；数据不可恢复性、
需求不确定性等作为可配置因子。AI 给出建议分、来源和理由，系统按公开公式计算，人工确认或记录调整原因。

## 8. 自动化候选

自动化适用性与风险分开评价，考虑重复频率、回归价值、判定确定性、环境可控性、数据成本、时间收益、
稳定性、开发成本、维护成本以及是否依赖人工观察或专业设备。

人工最终决定为：优先自动化、适合自动化、条件满足后自动化、保留人工执行。

首版不生成完整可运行脚本，但允许多个独立用例映射到同一 Pytest 参数化实现或任务模板，执行结果必须按
稳定用例 ID 分别返回。

## 9. 用例模板与用例模型

首版以 XLSX 为主、CSV 为辅。XLSX 可以包含多张用途不同的工作表。

- AI 建议工作表角色：用例表、说明表、字典表、统计表或未知。
- 用户选择参与处理的用例表，并逐表确认标题行和字段映射。
- 每张用例表可以使用不同映射。
- 一条用例只有一个主要归属表。
- 未参与处理的工作表在导出时保留。
- 首版不保证解析或维护复杂公式、宏、图片、合并单元格和数据透视表。

内部用例模型支持有序步骤、逐步预期和整体预期。一个明确输入或边界场景形成一条独立用例，不在一条用例中
内嵌多组测试数据。模板无法无损表达内部内容时必须提示人工确认，不得静默丢失。

用例设计方法和推导依据只在平台内部用于审核，不进入最终用例文件。

## 10. AI 多角色用例评审

三个角色分别发起独立 AI 运行：

- 产品经理：业务目标、用户价值、需求一致性和遗漏场景。
- 测试经理：覆盖充分性、可测试性、测试方法和自动化价值。
- 项目经理：依赖、资源、进度、阻塞风险和发布影响。

系统可以归并重复建议，但必须保留角色来源。人工逐条采纳、拒绝或修改；采纳建议形成新的用例修订版本，
不得覆盖原候选用例。

## 11. 用例身份、状态与导出

- 首次确认的用例获得稳定用例 ID。
- 内容修改生成不可覆盖的修订版本。
- 外部模板编号保留，但不替代内部 ID。
- 测试目标根本改变时创建新 ID，并将旧用例标记为被替代。

用例生命周期：草稿、有效、已关闭、已废弃、被替代。

当前测试参与状态：已纳入、本轮不测、待影响确认、待复测。

AI 只能提出状态建议。人工状态变更记录原状态、新状态、原因、时间和确认人。被替代必须关联承接用例，
已废弃必须填写原因，已关闭允许重新启用。

用例文件可以单独下载，支持当前全部、人工勾选、相对上一需求版本新增或修改三种范围。默认只导出已确认、
已纳入且为最新修订的用例。

## 12. 测试任务与执行适配

平台使用通用任务模型：任务 ID、用例引用、测试目标、优先级、前置条件、参数、预期结果、判定方式和证据要求。

执行目标可以是人工、项目一、通用 Pytest、其他外部执行器或暂未指定。目标专属配置放入目标扩展，
通过执行适配器转换 JSON/YAML。项目一是首个适配器，但不进入平台通用领域模型。

## 13. 执行批次与结果

执行批次记录产品版本、需求版本、环境、测试范围、负责人、目标执行方和用例修订快照。

创建批次时预分配执行序号。展示编号采用 `外部用例编号-执行序号`，例如 `TC-001-2`；内部仍分别保存
稳定用例 ID、修订版本、执行序号和唯一执行记录 ID。

执行状态包括通过、执行失败、阻塞、未执行和执行异常。执行失败表示获得有效结果但不符合预期；执行异常表示
因工具、环境、断电等原因没有获得有效测试结论。重试创建新记录，不覆盖历史。

一个批次可以导入多份人工、项目一或其他自动化结果。用户选择目标批次，系统只能建议归属。结果关联优先使用
稳定 ID 和修订版本，其次使用已登记外部编号，最后由人工匹配。

同一用例多次执行时同时展示初始结果、最新结果和人工确认的批次结论。人工与自动化结果冲突进入待处理，
不得由 AI 或时间顺序自动裁决。

## 14. 质量问题、覆盖与回归

首版只维护轻量质量问题引用：现象、证据、严重程度、问题模式、外部缺陷编号、状态、发布影响、修复版本和复测。
不实现完整缺陷管理工作流。

平台提供需求覆盖率、风险覆盖率、测试维度覆盖、执行覆盖率和自动化覆盖率。所有指标必须可展开查看分子、
分母和未覆盖明细；语义相似度不能直接建立覆盖事实。

回归候选首先由确定性规则产生，包括需求变化、高风险、历史失败或异常、结果冲突、未关闭质量问题、缺陷模式、
相邻状态和长期未执行用例。AI 可以附理由补充建议，人工确认最终回归选择。

## 15. AI 治理

四个人工确认关口为需求确认、设计确认、用例确认和发布确认。AI 不得越过关口自动推进。

每次 AI 运行不可覆盖，记录任务类型、模型与参数、Prompt 版本、输入资产版本、结构化输出、Schema 校验、
时间、状态、人工处置和 Mock 标记。只有通过结构校验的输出可进入人工确认流程。

采用独立模型适配层，硅基流动为默认真实服务，并提供本项目独立编写的确定性 Mock。密钥只由后端读取，
不得进入数据库、日志、报告或导出文件。仅对超时、限流和临时服务错误有限重试。

## 16. 技术路线

- 后端：Python、FastAPI、Pytest。
- 前端：React、TypeScript 和轻量前端测试工具。
- 数据库：SQLite。
- Excel：支持 XLSX 多工作表读取、映射与导出。
- 文档：DOCX、PDF、Markdown 和 TXT 解析。
- 契约：JSON Schema，支持 JSON/YAML。
- 部署：本地启动，不依赖云数据库。
- CI：默认使用 Mock AI，不调用真实模型。

## 17. 页面

1. 项目列表与仪表盘。
2. 项目工作台。
3. 需求评审。
4. 范围与风险。
5. 用例设计。
6. AI 多角色评审。
7. 用例发布。
8. 测试任务。
9. 执行批次与结果。
10. 治理分析。
11. 设置。

## 18. 演示数据

智能采集设备合成需求包覆盖设备生命周期、采集配置、状态流转、异常恢复、存储、连接、控制页面、提示与错误码、
安全、性能、稳定性、兼容性、升级回滚，以及视频、音频、IMU 和同步等数据专项。

- V1 约 30–40 条原子需求。
- 预埋 10–15 个评审问题和 15–25 个风险项。
- 约 80–120 条最终确认用例。
- V2 包含 8–12 个新增、修改或删除变更。
- 至少导入三份结果，包含失败后复测通过和一次结果冲突。
- 独立评估真值不进入正常模型调用。

## 19. 独立交付物

- 用例文件。
- JSON/YAML 测试任务。
- 测试设计 HTML 报告。
- 执行与治理 HTML 报告。
- JSON/ZIP 审计包。

## 20. 首版验收标准

- 多格式、多文件需求资料可形成版本化需求资产。
- 多工作表 XLSX 模板可逐表分类、映射并保留非用例表。
- 原子需求、来源引用、评审发现和四个人工关口可追踪。
- 范围、风险、优先级和自动化候选可解释并可人工调整。
- 三个 AI 角色独立评审，建议可逐条处置并形成修订版本。
- 用例可按原模板单独下载且不包含内部设计方法。
- 通用任务和项目一目标契约均通过 Schema 校验。
- 执行批次支持预分配序号、多文件导入、异常、阻塞、复测和冲突处理。
- 五类覆盖指标均能展开明细。
- V2 可触发影响分析、状态治理和人工回归选择。
- AI 运行、人工决定和资产来源可审计。
- Mock 模式支持离线演示和 CI。
- 不含原公司资产，也不重复实现项目一能力。

## 21. 持续维护文档

- [领域术语](./CONTEXT.md)
- [AI 调用与治理](./docs/ai-workflow.md)
- [课程应用映射](./docs/course-coverage.md)
- [项目介绍](./docs/project-introduction.md)
- [面试案例](./docs/interview-case.md)
- [工程日志](./docs/engineering-journal.md)
- [后续路线图](./docs/roadmap.md)
- [合成需求计划](./docs/synthetic-requirements-plan.md)
- [资产来源清单](./docs/asset-provenance.md)
- [架构决策](./docs/adr/)
